# 更新日志

本文档记录 EasyChat 的重要变更。EasyChat 从 `1.0.0` 开始维护独立版本线。

## [1.0.4] - 2026-08-02

### 修复

- 修复插件发布后普通用户仍需手动启用的问题：发布时为所有非管理员用户默认启用，新注册或后台新建的非管理员用户自动启用已发布插件，同时保留用户已有的启停状态并排除管理员账号。

## [1.0.3] - 2026-08-02

### 新增

- 聊天图片支持点击查看原图，桌面端可滚轮缩放和拖拽，移动端支持双指缩放，并提供适应窗口、原始尺寸和下载操作。
- PWA 启用自动更新和旧缓存清理，降低新版本仍加载旧前端资源的概率。

### 优化

- 统一聊天发送、重新生成和自动续写的流式响应处理，减少重复代码及不同请求路径的行为差异。
- 优化 AI 回复的图文排版：文字与 Markdown 图片紧邻时补充换行，并避免已有独立图片段落重复叠加间距。
- 默认模型选择器适配移动端可用宽度，避免设置对话框中的内容被截断。
- 高级模式手动绘图使用当前会话选择的模型，保留 MG 服务优先和 OpenAI 兼容图片接口回退逻辑。

### 修复

- 区分模型输入图片和用户本次显式附件，防止自动回填的历史参考图在刷新后错误显示为用户上传内容。
- 修复分别删除用户消息与 AI 回复时状态未完整转换、刷新后已删除消息重新出现的问题；删除失败时不再提前移除本地消息。
- 修复清空会话、删除房间及房间设置保存失败时前端状态与数据库不一致的问题，并为批量删除使用带回滚的 SQLite 事务。
- 为聊天记录增加用户归属及旧数据库安全迁移，所有读取、写入、删除、清空、重新生成和中止操作均校验当前用户，避免相同房间编号导致跨用户数据串用。
- 房间不存在时拒绝聊天请求，避免先写入孤立记录后才返回错误。
- 邮箱验证及密码重置链接增加 12 小时有效期校验；新密码改用随机盐 `scrypt`，旧 MD5 密码在成功登录后自动升级。
- 密码重置等待数据库更新完成后再返回成功，管理员用户列表不再返回密码哈希。

## [1.0.2] - 2026-08-02

### 新增

- 支持镜像内置插件与部署目录外挂插件同时加载；插件 ID 相同时优先使用外挂版本，便于覆盖升级内置插件。

### 优化

- 设置界面统一采用固定最大宽度、视口自适应高度和标签内容区独立滚动，避免窗口拉宽后布局松散。
- 优化用户、Keys 和 Prompt 二级弹窗的响应式尺寸、表单密度与移动端标签布局。
- API Key 和接口地址改为紧凑的单行输入控件，保存操作统一移至弹窗页脚。
- 移除统计页多余的 `Usage intelligence` 标题行。

### 修复

- 修复统计内容较长时由全局页面滚动、缩小窗口后下方内容不可见的问题。
- 修复用户权限和模型选择下拉菜单被二级弹窗边界裁剪的问题。

## [1.0.1] - 2026-08-01

### 新增

- 设置界面新增“关于”页面，展示当前版本、项目地址、问题反馈入口和开源许可。

### 优化

- PWA 标题栏主题色跟随明暗主题切换，提升独立窗口模式下的视觉一致性。
- GitHub Actions 构建环境统一升级为 Node.js 24。
- 更新 README 的界面截图与使用说明，并减少 Vite 对非源码目录的不必要监控。

## [1.0.0] - 2026-08-01

EasyChat 首个独立版本，以中文自托管场景为默认使用方式。

### 新增

- 支持通过 OpenAI 兼容接口连接多个模型供应商和自定义 API Base URL。
- 支持多会话、上下文续聊、流式响应、思考内容展示和手动停止生成。
- 支持自动生成会话标题，并在模型不可用时使用本地标题回退。
- 支持图片上传、图文对话、历史图片回填和多轮图片追问。
- 支持多 Key 管理、上游模型刷新、角色权限和随机 Key 选择。
- 支持用户注册登录、邮件验证、管理员审核和用户管理。
- 支持请求数、Token、用户排行和模型分布等使用量统计。
- 新增插件运行时与 `@easychat/plugin-sdk`，支持 Function Call 工具、插件发布、用户启用和全局设置。
- 内置图片生成插件，支持管理员统一选择模型。
- 支持简体中文、繁体中文和英文界面，默认使用简体中文。
- 支持 Docker、Docker Compose、PWA 和 GitHub Actions 构建流程。

### 调整

- 项目名称、包名、插件 SDK、Docker 镜像和部署资源统一为 EasyChat。
- 页面和中文邮件品牌固定为“简聊”，英文界面使用 `EasyChat`。
- 移除动态 `SITE_TITLE` 配置和容器启动时的标题替换脚本。
- Node.js 统一为 `24.18.0`，pnpm 统一为 `9.15.9`。
- 前端默认开发端口为 `10002`，后端默认端口为 `3002`。
- 清理未使用的旧组件、无效 Git hooks 配置和遗留部署忽略项。
- Docker Compose 将部署目录下的插件只读挂载到容器，便于独立更新插件。
- 升级 Axios、Multer、SQLite、Vite 和 PWA 等直接依赖，移除未使用的 CryptoJS。
- 补全 PWA 主题色、背景色和独立窗口安装配置。
- 设置对话框新增“关于”页面，展示当前版本、项目地址和问题反馈入口。

### 文档

- 重写中英文 README，更新开发、配置、构建和部署说明。
- 新增当前工程架构、模型权限、插件运行时和核心请求链路说明。

## 上游历史

EasyChat 基于 [Chanzhaoyu/chatgpt-web](https://github.com/Chanzhaoyu/chatgpt-web) 演进。迁移前的版本记录和贡献历史请查阅上游仓库；相关版权与许可声明见 [LICENSE](./LICENSE)。
